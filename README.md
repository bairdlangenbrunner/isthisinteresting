personal blog Next.js site by Baird Langenbrunner

```git clone``` this repo and then do npm install within it

```npm run dev``` will let you locally host it  
```npm build``` > copy/paste the contents in the out folder